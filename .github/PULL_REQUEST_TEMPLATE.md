## What changed

## What you ran

- [ ] `pnpm typecheck && pnpm test && pnpm lint`
- [ ] `pnpm test:live` (if `jev.ts`, `skeleton.ts`, `engine.ts`, the CLI or the MCP server changed)

## Report before / after (for decision-logic changes)
